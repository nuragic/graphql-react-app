# React app with GraphQL

A simple React app with GraphQL, just for testing.

## Usage

Clone the repo and run `npm start`.

If you want to test this app with an example server, you can use https://github.com/nuragic/mercurius-playground.
